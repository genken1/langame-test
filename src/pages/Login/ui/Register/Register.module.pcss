.register {
    display: flex;
    flex-direction: column;
    align-items: center;

    &Text {
        font-family: "Lato Regular", sans-serif;
        font-size: 13px;
    }

    &Button {
        font-family: "Lato Bold", sans-serif;
        font-size: 14px;
        color: var(--primary);
    }
}
